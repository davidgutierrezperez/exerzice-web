{% extends 'base.twig' %}

{% block styles %}
    {{ parent() }}
    <link rel='stylesheet' href='/css/pages/index.css'>
{% endblock %}

{% block content %}
    <header>
        {% include 'components/search/search-bar.twig' %}
    </header>
    {% include 'components/navigation/aside-nav.twig' %}

    <section class='posts-section'>
        {% if posts %}
            {% for post in posts %}
                {% include 'components/posts/post.twig' with {post: post} %}
            {% endfor %}
        {% else %}
            <p class='error'>No posts were found</p>
        {% endif %}
    </section>

    <aside>
        {% if spaces %}
            {% for space in spaces %}
                {% include 'components/community/community-summary.twig' with {community: space} %}
            {% endfor %}
        {% else %}
            <p class='error'>No communities were found</p>
        {% endif %}
    </aside>
{% endblock %}